package kz.fearsom.financiallifev2.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Primary Palette ─────────────────────────────────────────────────────────
val GoldPrimary    = Color(0xFFFFD700)
val GoldLight      = Color(0xFFFFE566)
val GoldDark       = Color(0xFFB8960C)

val GreenSuccess   = Color(0xFF00E676)
val GreenMedium    = Color(0xFF00C853)

val RedDanger      = Color(0xFFFF5252)
val RedLight       = Color(0xFFFF8A80)

val BlueAccent     = Color(0xFF40C4FF)
val PurpleAccent   = Color(0xFFB388FF)

// ─── Dark Background ─────────────────────────────────────────────────────────
val BackgroundDeep     = Color(0xFF0A0E1A)
val BackgroundCard     = Color(0xFF111827)
val BackgroundElevated = Color(0xFF1C2539)
val BackgroundChat     = Color(0xFF0D1424)

val SurfaceGlass       = Color(0x1AFFFFFF)
val SurfaceGlassBorder = Color(0x33FFFFFF)

// ─── Dark Text ────────────────────────────────────────────────────────────────
val TextPrimary   = Color(0xFFF0F4FF)
val TextSecondary = Color(0xFF8899BB)
val TextHint      = Color(0xFF445577)

// ─── Light Text ───────────────────────────────────────────────────────────────
val TextPrimaryLight   = Color(0xFF0A0E1A)
val TextSecondaryLight = Color(0xFF556B8A)
val TextHintLight      = Color(0xFF8899BB)

// ─── Light Mode Backgrounds ───────────────────────────────────────────────────
val BackgroundLightDeep     = Color(0xFFFAFBFC)
val BackgroundLightCard     = Color(0xFFFFFFFF)
val BackgroundLightElevated = Color(0xFFF3F5F9)
val BackgroundLightChat     = Color(0xFFF8FAFD)

val SurfaceGlassLight       = Color(0x1A000000)
val SurfaceGlassBorderLight = Color(0x33000000)

// ─── Message Bubbles (Dark Mode) ──────────────────────────────────────────────
val BubbleCharacter = Color(0xFF1A2540)   // Asan's left-side bubble
val BubblePlayer    = Color(0xFF0D1F3C)   // Player's right-side choice bubble
val BubbleReport    = Color(0xFF0E231A)   // Monthly financial report card
val BubbleSystem    = Color(0xFF251A3A)   // System announcement bubble

// ─── Message Bubbles (Light Mode) ─────────────────────────────────────────────
val BubbleCharacterLight = Color(0xFFE8F0FF)
val BubblePlayerLight    = Color(0xFFF0F5FF)
val BubbleReportLight    = Color(0xFFE8F5F0)
val BubbleSystemLight    = Color(0xFFF5E8FF)

// ─── Stats ────────────────────────────────────────────────────────────────────
val StatCapital   = Color(0xFFFFD700)
val StatDebt      = Color(0xFFFF5252)
val StatStress    = Color(0xFFFF6E40)
val StatKnowledge = Color(0xFF40C4FF)
val StatRisk      = Color(0xFFB388FF)
